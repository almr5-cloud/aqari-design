# 🏠 عقاري ديزاين / Aqari Design — ملف التسليم

> **آخر تحديث: 12 أغسطس 2026**
> هذا الملف مكتوب ليُفهم المشروع من الصفر بلا الرجوع لأي محادثة. اقرأه كاملاً قبل أي تعديل.

> 🔴 **اقرأ §4 أولاً.** لغز «صفر تحميلات» انحلّ في 12 أغسطس: التحميل نفسه كان
> مكسوراً على الجوال. أُصلح. وبقيت مشكلة أخطر لم يُتحقق منها بعد: هل يستدعي
> Whop الجسر أصلاً عند الدفع؟

---

## 0. ما هو المشروع في سطرين

منصة ويب تتيح للوسيط العقاري تحويل بيانات عقاره إلى **منشور إنستقرام/فيسبوك احترافي في أقل من دقيقة**، بلا خبرة تصميم وبلا تحميل أي برنامج.

يعمل بنسختين مستقلتين: **عربية** للسوق السعودي و**إنجليزية** للسوق الأمريكي.

| | |
|---|---|
| الموقع | https://aqaridesign.com (عربي) · https://aqaridesign.com/en/ (إنجليزي) |
| المستودع | https://github.com/almr5-cloud/aqari-design — ⚠️ **عام/Public** |
| المجلد المحلي | `C:\Users\Admin\Desktop\برمجت موقع عقاري` |
| لوحة الإحصاءات | `https://aqaridesign.com/stats.html` ثم تلصق المفتاح (محفوظ عندك) |
| السعر | **49 ريال/شهر** (عربي) · **$19/شهر** (إنجليزي) — 30 تصميماً شهرياً |
| البريد | aqaridesign@gmail.com |
| مالك المشروع | مؤسس فرد في السعودية |

---

## 1. وين وصلنا بالضبط

### ✅ يعمل بالكامل

**المنتج (النسختان)**
- 6 قوالب رسم (عصري · فاخر · نظيف · بطاقة · معرض · شريطي) × 3 مقاسات (1080×1080 · 1080×1350 · 1080×1920)
- **اللصق الذكي** — الميزة المميزة: يلصق الوسيط نص إعلانه الخام فتُستخرج الحقول تلقائياً. نسخة عربية ونسخة إنجليزية، كل واحدة محلل مستقل
- رفع صورة العقار مع تكبير وتحريك · شعار المكتب · لون الهوية · حقول إضافية ديناميكية
- **حقول التوافق الأمريكية** (النسخة الإنجليزية فقط): اسم المكتب Brokerage · رقم الرخصة · شعار Equal Housing Opportunity — تُرسم شريطاً سفلياً على القوالب الستة
- حسابات (تسجيل فوري، تأكيد البريد معطّل عمداً) · حفظ سحابي · خدمة عملاء بالبريد

**التشغيل**
- الدفع: Whop — خطة ريال وخطة دولار **منفصلتان**، وتفعيل تلقائي عبر Webhook
- لوحة إحصاءات خاصة: مسار العميل · الأجهزة · المصادر · العملاء ونشاطهم · فحص SEO/AEO مباشر
- SEO 10/10 و AEO 7/7 · hreflang متبادل · sitemap للغتين
- 5 إعلانات جاهزة في `ads/`

### 📊 الأرقام الحقيقية (10 أغسطس 2026، بعد استبعاد الزيارات الداخلية)

| | |
|---|---|
| زيارات 7 أيام | 191 |
| دخلوا المحرّر | 25 |
| فتحوا التسجيل | 41 |
| **اصطدموا ببوابة الدفع** | **37** |
| أنشأوا حساباً | **2** |
| حسابات حقيقية (إجمالي) | **3** |
| اشتراكات نشطة | **1** — `salem50045004@hotmail.com` |
| **تحميلات العملاء الحقيقيين** | **0** — والسبب معروف الآن، انظر §4 |

> ⚠️ الأرقام قبل 10 أغسطس ملوّثة بزيارات التطوير ولا يمكن فصلها بأثر رجعي.

> 📌 **تحديث 12 أغسطس:** الصفر ليس لأن أحداً لم يحاول. سالم حاول **15 مرة في
> 12 دقيقة** (10 منها سُجّلت في القاعدة) ولم تصله صورة واحدة. أُصلح العطل
> وأُعيد رصيده. لا تقرأ هذا الصفر كإشارة سوق — هو أثر عطل برمجي.

### 🔨 آخر ما أُنجز (بالترتيب)
1. النسخة الإنجليزية كاملة على `/en/` — هبوط ومحرّر وصفحة About
2. حقول التوافق الأمريكية + شريط EHO على القوالب الستة
3. خطة دولار منفصلة في Whop ($19) وربطها بالمحرّر الإنجليزي
4. **اللصق الذكي الإنجليزي** — اختُبر على 6 إعلانات واقعية وأُصلحت 6 أخطاء كشفها الاختبار
5. تدقيق ما قبل التسويق: 65 فحصاً آلياً · 0 فشل
6. **فلتر الزيارات الداخلية** — أرقام اللوحة صارت للعملاء فقط
7. 5 إعلانات للسوق الأمريكي + أداة Whop CLI مثبتة

**12 أغسطس:**
8. AEO ثنائي اللغة: `llms.txt` كان عربياً بالكامل ويعلن «السوق: السعودية» — أُعيد بناؤه بقسم إنجليزي كامل · سكيما المنظمة وُحّدت على `areaServed: ["US","SA"]` · `hreflang` مُصلَح · خريطة الموقع أُرسلت لجوجل وطُلبت فهرسة الصفحات الإنجليزية
9. **البيت الأمريكي** بدل المبنى الخليجي في عروض النسخة الإنجليزية والإعلانين 1 و4 (الصورة من Pexels برخصة تجارية — صور Zillow **لا تصلح**)
10. تدقيق ما قبل التسويق: **تراكب في قالبَي `clean` و`gallery`** عند إضافة ميزة رابعة · بقايا RTL في واجهة إنجليزية · `logo.png` من 502 ك.ب إلى 16 · اللصق الذكي 6/6
11. 🔴 **إصلاح عطل التحميل** — انظر §4 و§9



---

## 2. الملفات ودور كل ملف

### تُنشر على الموقع

| الملف | الدور |
|---|---|
| `index.html` | **صفحة الهبوط العربية** — تبيع قبل ما تطلب التسجيل: هيرو · منزلق قبل/بعد · عرض اللصق الذكي · خطوات · قوالب · سعر · أسئلة شائعة |
| `app.html` | **المحرّر العربي** (RTL) — الواجهة والقوالب الستة والمحلل العربي والحسابات والاشتراك وخدمة العملاء والتتبع |
| `about.html` | صفحة محتوى عربية غنية — **مهمة للـSEO/AEO** (يقتبس منها الذكاء الاصطناعي) |
| `en/index.html` | **صفحة الهبوط الإنجليزية** |
| `en/app.html` | **المحرّر الإنجليزي** (LTR) — نسخة مستقلة تماماً |
| `en/about.html` | صفحة محتوى إنجليزية (~900 كلمة) للـAEO الأمريكي |
| `stats.html` | **لوحة المالك** — محمية بمفتاح، `noindex`، محجوبة في robots |
| `demo-*.jpg` · `en/demo-*.jpg` | صور منزلق «قبل/بعد» في صفحتَي الهبوط |
| `ads/ad-1..5.jpg` | خمسة إعلانات 1080×1080 للسوق الأمريكي |
| `logo.png` · `og-image.png` | الشعار وصورة معاينة المشاركة |
| `tutorial.mp4` · `cancel-tutorial.mp4` | فيديو الاستخدام وفيديو الإلغاء (عربي فقط) |
| `fonts/*.woff2` | خط ثمانية — Sans للنصوص، Serif Display للأسعار (النسخة العربية فقط) |
| `robots.txt` | يسمح لزواحف الذكاء الاصطناعي، يحجب `stats.html` |
| `sitemap.xml` · `llms.txt` | خريطة الموقع للغتين + ملخص منظّم لنماذج الذكاء الاصطناعي |
| `CNAME` | يربط GitHub Pages بالدومين — **لا تحذفه أبداً** |

### تُنفَّذ في Supabase (ليست على الموقع)

| الملف | الدور |
|---|---|
| `supabase/functions/whop-webhook/index.ts` | **جسر الدفع** — يستقبل إشعارات Whop، يستخرج بريد العميل، يفعّل/يلغي الاشتراك، ويسجّل كل إشعار في `webhook_log` |
| `supabase-setup.sql` | جدولا `profiles` و`designs` + سياسات RLS |
| `subscriptions-setup.sql` | جدول `subscriptions` |
| `downloads-setup.sql` | جدول `downloads` (لحد الـ30 شهرياً) |
| `whop-automation.sql` | دالة `set_subscription_by_email` — للخادم فقط |
| `customers-panel.sql` | دالة `get_stats` التي تغذّي اللوحة |

### على الجهاز فقط (مستثناة من Git)
`_ads.html` (مصدر الإعلانات) · `_before.html` (مولّد صورة «قبل») · `_prop.png` · `كاروسيل/` · الكتيّب PDF · لقطات الشاشة والتصاميم المُصدَّرة

### جداول قاعدة البيانات
`profiles` · `designs` · `subscriptions` · `downloads` · `events` · `webhook_log`

---

## 3. القرارات التقنية ولماذا

### بلا إطار عمل — كل صفحة ملف HTML واحد
كل شيء (HTML + CSS + JS) في ملف واحد لكل صفحة. **السبب الأصلي**: الجهاز لم يكن فيه Node.js. النتيجة التي بقيت مفيدة: **نشر فوري بلا خطوة بناء**، وتحميل شبه لحظي.
> ملاحظة: Node.js و npm **مثبتان الآن** (v24 / v11) — لكن لا تُدخل أداة بناء بلا سبب قوي؛ البساطة هنا ميزة.

### الرسم على Canvas في متصفح العميل
التصميم يُرسم عند الزائر لا على خادم. **السبب**: تكلفة خادم صفر (حاسم لهامش 49 ريال)، ومعاينة فورية، والصورة المُصدَّرة مطابقة تماماً لما يراه.

### Supabase وليس Firebase
قاعدة علائقية (PostgreSQL) تناسب النموذج (مستخدم ← اشتراك ← تصاميم)، وحماية RLS على مستوى الصف، وتسعير متوقع. Firebase (NoSQL) يعقّد هذه العلاقات.

### Whop للدفع وليس Stripe أو ميسر
- **Stripe**: لا يقبل تاجراً مقيماً في السعودية بلا كيان في دولة مدعومة
- **ميسر/Paylink**: أفضل تقنياً (تدعم **مدى**) لكن تتطلب سجلاً تجارياً
- **Whop**: يقبل بائعاً فرداً بلا سجل تجاري، ويدعم الاشتراكات المتجددة
- ⚠️ **العيب المقبول**: لا يدعم مدى · رسومه ~4-6%

### خطتان منفصلتان لا خطة موحّدة
`app.html` → خطة الريال · `en/app.html` → خطة الدولار.
**السبب**: توحيدهما يعني إما رفع سعر السعوديين 45% أو خسارة 32% من الأمريكيين، وفي الحالتين رسوم تحويل عملة على أحد الطرفين. الجسر يفعّل **بالبريد لا بالخطة**، فلا يحتاج إعداداً إضافياً.

### نسختان منفصلتان لا نظام لغتين (i18n)
`app.html` و`en/app.html` **ملفان مستقلان**. **السبب**: النسخة العربية فيها عميل يدفع، وإعادة هيكلتها مخاطرة غير مبررة.
> ⚠️ **الثمن**: أي إصلاح في محرّك الرسم **يجب تطبيقه على الملفين**. راجع هذا القرار لو تعددت الأسواق.

### GitHub Pages للاستضافة
مجاني · شبكة توزيع عالمية · HTTPS تلقائي · نشر بمجرد `git push`.

### خط ثمانية محلياً وليس Google Fonts
يعمل بلا إنترنت، وهوية عربية احترافية. النسخة الإنجليزية تستخدم مكدّساً لاتينياً من النظام (لا تحمّل خطوطاً).

### لا خطة مجانية
التصميم والمعاينة مجاناً بلا تسجيل؛ الاشتراك مطلوب **للتحميل والحفظ**. قرار المالك.

---

## 4. المشاكل المفتوحة

### ✅ [انحلّ 12 أغسطس] لغز «صفر تحميلات» — كان عطلاً برمجياً لا عزوفاً
بقي هذا شهراً يُقرأ كإشارة سوق («المنتج لا يُستخدم»)، وكان في الحقيقة **ثلاثة
أعطال متراكبة في دالة `download`، في النسختين معاً**:

1. `toDataURL` لصورة 1080×1080 ينتج نصاً بطول ~2 ميغابايت. متصفحات الجوال —
   وخصوصاً المدمجة داخل إنستقرام وواتساب — ترفض تنزيل روابط `data:` بهذا
   الحجم، **بلا أي رسالة خطأ**. صار عبر `Blob` و`createObjectURL`.
2. وسم `<a>` لم يكن مُضافاً إلى الصفحة، وكثير من المتصفحات تتجاهل خاصية
   `download` على وسم غير متصل بالمستند. صار يُضاف ثم يُزال.
3. **الأخطر:** `downloadsCount++` كان ينفَّذ فور `a.click()` بلا تحقق — فيخسر
   المشترك من حصته الشهرية مقابل ملف لم يصله. صار الخصم داخل رد نداء
   `toBlob` بعد التأكد من إنتاج الملف.

**كيف انكشف:** العميل الدافع كتب «رفض يحمل الصوره ويحسب علي». لولا رسالته
لبقي الخطأ مخفياً — راجع §9 الدرس 8.

**أُضيف أيضاً:** إعادة محاولة تسجيل التحميل بعد `refreshSession` (رمز الجلسة
يصلح ساعة، وصفحة بقيت مفتوحة تفشل عندها سياسة `auth.uid() = user_id`)، ورابط
احتياطي «افتح الصورة» لمتصفحات التطبيقات المدمجة، وإظهار نص الخطأ بدل إخفائه.

**تحقّق:** اختبار متصفح ينتج `image/png` بحجم ~977 ك.ب في النسختين، والمالك
جرّب من جواله فنزلت الصورة ونقص العدّاد 20←19.

### 🔴 نزف عند التسجيل
`191 زيارة → 41 فتحوا التسجيل → حسابان`. و**37 اصطدموا ببوابة الدفع** — أي صمّموا شيئاً وأرادوا تحميله.
القيمة مثبتة؛ آلية التحويل مكسورة. الفرضية الأرجح: يبذل الجهد كاملاً ثم يُمنع لحظة الحصاد.
**خيار مطروح (قرار المالك)**: أول تحميل مجاني بعد إنشاء حساب، ثم الاشتراك.

### 🔴 Whop لم يستدعِ الجسر ولا مرة واحدة — أخطر مجهول في المشروع
فُحص `webhook_log` يوم 12 أغسطس: **11 صفاً، كلها بتاريخ 6 أغسطس، وكلها دفعات
اختبار كتبناها بأيدينا.** الدليل قاطع — معرّفات العضوية فيها `mem_123` و`m1`
و`m2` و`m3` و`u_abc`، ومعرّفات Whop الحقيقية سلاسل عشوائية طويلة.

**المعنى:** تفعيل سالم جاء من دفعة اختبار أرسلناها نحن تحمل بريده — أي تفعيل
يدوي بثوب تلقائي. ولا يوجد **أي** دليل على أن Whop مربوط بالجسر فعلاً.

**الخطر على العميل:** يدفع 19 دولاراً → لا يُفعَّل حسابه → يبقى يرى «اشترك
الآن» → يطلب استرجاعاً ويكتب مراجعة سيئة. ولن تعرف إلا إذا اشتكى.

**الجسر نفسه سليم** — مختبَر من طرف إلى طرف: يستخرج البريد من أشكال payload
متعددة، ويتجاهل الأحداث المجهولة بأمان، ويسجّل كل استدعاء.

**كيف تتحقق (دقيقتان):** لوحة Whop ← Developer/Webhooks ← تأكد أن الرابط موجود
وفيه الأحداث الثلاثة ← **Send test** ← ثم اقرأ `webhook_log` وابحث عن صف جديد.
> ⚠️ `whop.com` محجوب على المتصفح الآلي — هذا الفحص يدوي بالضرورة.

### 🟠 مفتاح الـwebhook ظهر في محادثة
`WEBHOOK_KEY` كُتب نصاً في محادثة. من يملكه يستطيع تفعيل اشتراك مجاني لأي بريد مسجّل.
**التدوير**: Supabase → Edge Functions → Secrets → غيّر القيمة، ثم حدّث الرابط في Whop بالمفتاح الجديد.

### 🟠 لا يوجد Meta Pixel
بدونه لا تحسين حملات ولا retargeting ولا قياس عائد داخل Meta. **يجب تركيبه قبل الصرف على الإعلانات.**

### 🟠 ازدواج البريد عند الدفع — حصل فعلاً لا نظرياً
سجل `webhook_log` يُظهر أن `salem7006@hotmail.com` أُلغي اشتراكه بينما
`salem50045004@hotmail.com` فُعِّل — **نفس الشخص بحسابين**. لو دفع العميل ببريد
غير بريد تسجيله (شائع مع Apple Pay) فلن يُفعَّل. النافذة التحذيرية `showPayGuard`
تخفف ولا تحل. **الحل الجذري المؤجَّل:** تسجيل الدخول عبر Whop OAuth.
عند التسويق: شدّد في رسالتك أن يدفع بنفس بريد التسجيل.

### 🟡 ملفان يجب تحديثهما معاً
أي تعديل على محرّك الرسم يلزم تكراره في `app.html` و`en/app.html`.
> عطل التحميل أعلاه كان في **الملفين معاً** — وهذا ثمن قرار «نسختان لا i18n» (§3).

### 🟡 بوابة الاشتراك تُفحص في المتصفح
مبرمج متمكن يتجاوزها. مقبول للانطلاق؛ التحصين يحتاج توليد الصورة على الخادم.

### 🟡 أداة Whop CLI مثبتة وغير مسجّلة الدخول
انظر §6.

---

## 5. الخطوة الجاية (بالترتيب)

**1️⃣ 🔴 تحقق من webhook في Whop قبل أي تسويق** — Send test ثم اقرأ `webhook_log`.
لا معنى لجلب عميل أمريكي إن كان الدفع لا يفعّل حسابه. هذه أهم خطوة في الملف كله.

**2️⃣ أكّد مع سالم أن التحميل صار يعمل** — أُصلح العطل وأُعيد رصيده (30 كاملة).
ردّه هو الاختبار الحقيقي الوحيد: على جهازه وشبكته، وهي التي كشفت العطل أصلاً.
وعُرض عليه استرجاع كامل — قرار المالك إن أصرّ.

**3️⃣ أصلح نزف التسجيل** — 37 شخصاً أرادوا التحميل. هذا الرقم يتحسن في السوقين معاً.

**4️⃣ ركّب Meta Pixel** — قبل أي دولار على الإعلانات.

**5️⃣ التسويق الأمريكي**: ابدأ بـ**اشتغل قبل أن تطلب** — اختر مدينة واحدة (Austin مثلاً)، صمّم إعلاناً لوسيط حقيقي من منشوره، وأرسله له بلا رابط في أول رسالة. 100 رسالة ← متوقع 2-4 مشتركين.

**6️⃣ التسويق العربي**: قروب تلقرام (1,635 عضو) وواتساب (98) — أكبر فرصة غير مستغلة.

**مؤجّل**: تسجيل دخول عبر Whop OAuth (يلغي مشكلة ازدواج البريد) · SMTP خارجي · صفحة about إنجليزية موسّعة.

---

## 6. الأوامر وخطوات التشغيل

### التطوير المحلي
لا سيرفر ولا بناء — **افتح `index.html` في المتصفح مباشرة**.

### النشر (بعد أي تعديل)
```bash
cd "C:/Users/Admin/Desktop/برمجت موقع عقاري"
git add -A
git commit -m "وصف التعديل"
git push
git branch -f gh-pages main
git push origin gh-pages -f
```
> ⚠️ **الموقع يُنشر من فرع `gh-pages`** — لا تنسَ مزامنته، وإلا لن يظهر التعديل.

**متابعة النشر** (يستغرق 1-3 دقائق):
```bash
gh api repos/almr5-cloud/aqari-design/pages/builds/latest --jq '.status + " " + .commit[0:7]'
```
قارن الـcommit بـ`git rev-parse --short HEAD` — لو مختلفان فالبناء لم يبدأ بعد.

**تحفيز بناء يدوياً**:
```bash
gh api -X POST repos/almr5-cloud/aqari-design/pages/builds
```

### قراءة الإحصاءات من الطرفية
```bash
curl -s -X POST "https://pjhafrhjmvyirrcmnrhc.supabase.co/rest/v1/rpc/get_stats" \
  -H "apikey: <PUBLISHABLE_KEY>" -H "Content-Type: application/json" \
  -d '{"p_key":"<STATS_KEY>"}'
```

### استبعاد زياراتك من الإحصاءات
افتح مرة واحدة على **كل جهاز** تستخدمه:
```
https://aqaridesign.com/?internal=1
```
تُوسم زياراتك `internal` وتُستبعد. للإلغاء `?internal=0`. تحقق عبر `internal_excluded_7d`.

### تفعيل اشتراك يدوياً (لو فشل التلقائي)
```bash
curl -X POST "https://pjhafrhjmvyirrcmnrhc.supabase.co/functions/v1/whop-webhook?key=<WEBHOOK_KEY>" \
  -H "Content-Type: application/json" \
  -d '{"action":"membership_activated","data":{"id":"manual","user":{"email":"بريد_العميل"}}}'
```
الرد المتوقع `OK: activated:<uuid>` · و`user_not_found` يعني البريد غير مسجّل في الموقع.

### أداة Whop في الطرفية
```bash
npm install -g @whop/cli      # الرسمية — whopio/whop-public-cli
```
> ⚠️ **لا تثبّت حزمة `whop` المجرّدة** — لمطوّر آخر ومهجورة منذ 2022.
> ولا تستخدم `curl -fsSL https://whop.com/install.sh | sh` — سكربت لِلينكس على جهاز ويندوز.

**تسجيل الدخول** (معلّق): النافذة الحالية ترث PATH قديماً. **أعد تشغيل الجهاز مرة واحدة** ثم:
```bash
whop auth login
whop auth status      # لازم loggedIn: true
```
أو بلا إعادة تشغيل:
```bash
& "$env:APPDATA\npm\whop.cmd" auth login
```
أهم أمرين بعدها: `whop webhooks list` و`whop stats`.

### تعديل قاعدة البيانات
Supabase → SQL Editor → الصق ملف `.sql` → Run.
> 🐞 **فخ معروف**: المحرّر لا يُحمَّل إذا كان تبويب كروم **مخفياً أو في الخلفية**. اجعل النافذة ظاهرة أمامك.
> 🐞 اكتب SQL **في سطر واحد** — المحاذاة التلقائية تفسد الأسطر المتعددة.

### تعديل جسر Whop
Supabase → Edge Functions → `whop-webhook` → Code → عدّل → **Deploy updates**.
> ⚠️ **لا تستخدم رموز الهروب** (`\w`, `\.`) في التعبيرات النمطية هنا. لصقها عبر أدوات وسيطة كسرها مرة، فتوقف استخراج البريد وأُهملت كل الإشعارات بصمت — وكلّف ذلك أول عميل. استخدم `[A-Za-z0-9]` و`[.]`.
> ⚠️ **حدّث الملف في المستودع في نفس اليوم** — كان قديماً ذات مرة وكاد يعيد العطل نفسه.

### الدومين
Namecheap → Advanced DNS:
```
A     @    185.199.108.153
A     @    185.199.109.153
A     @    185.199.110.153
A     @    185.199.111.153
CNAME www  almr5-cloud.github.io
```
**ينتهي 18 يوليو 2027** — فعّل التجديد التلقائي.
> 🐞 ظهرت «صفحة بيضاء» مرة بسبب **إيقاف الدومين لعدم تأكيد بريد التسجيل** (متطلب ICANN)، لا بسبب DNS. راقب بريد Namecheap.

### إعادة توليد الصور والإعلانات
```bash
# الإعلانات الخمسة (HTML ثابت — يعمل في Edge بلا واجهة)
msedge --headless=new --window-size=1080,1080 --screenshot="ads/ad-1.png" \
  --allow-file-access-from-files --virtual-time-budget=6000 "_ads.html?s=1"
```
(مسار Edge: `C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe`)

> 🐞 **مهم**: Edge بلا واجهة **لا ينفّذ حدث `load`** قبل اللقطة، فلا يصلح لأي صفحة تعتمد canvas (مثل المحرّر).
> الطريقة الناجحة لتوليد صور من المحرّك: نسخة من المحرّر بالصور مدمجة كـ`data:` → تُفتح في متصفح حقيقي → `canvas.toDataURL` → تُرسل إلى خادم بايثون محلي على `127.0.0.1:8765` يكتب الملف.
> HTML الثابت (بلا canvas) يعمل في Edge بلا مشاكل.

---

## 7. المفاتيح والأسرار

> ⚠️ **لا تكتب أي سر في هذا المستودع — هو عام.** ولا تلصقه في محادثة.

| السر | مكانه |
|---|---|
| Supabase URL + publishable key | مكتوبان في صفحات الموقع — **آمنان بالتصميم** (الحماية بـRLS) |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase → Settings → API — سري |
| `WEBHOOK_KEY` | Supabase → Edge Functions → **Secrets** |
| مفتاح لوحة الإحصاءات | داخل دالة `get_stats` + في متصفحك |

**معرّف مشروع Supabase**: `pjhafrhjmvyirrcmnrhc`

**تدوير مفتاح اللوحة** (سطر واحد في SQL Editor):
```sql
do $$ declare src text; begin select pg_get_functiondef(p.oid) into src from pg_proc p join pg_namespace n on n.oid = p.pronamespace where n.nspname='public' and p.proname='get_stats'; src := replace(src,'القديم','الجديد'); execute src; end $$;
```

---

## 8. ثوابت مهمة في الكود

| الثابت | العربي | الإنجليزي |
|---|---|---|
| `PAYMENT_LINK` | خطة 49 ريال | خطة $19 |
| `PRO_MONTHLY_DOWNLOADS` | `30` | `30` |
| `SUPPORT_EMAIL` | aqaridesign@gmail.com | نفسه |

**أحداث التتبع المسموحة**: `visit` (الهبوط) · `app_open` (المحرّر) · `seen` · `auth_open` · `signup` · `pay_click` · `paywall` · `download` · `support_open` · `support_click`

> ⚠️ لإضافة نوع حدث جديد **يجب** تحديث سياسة RLS واسمها `anon insert events`، وإلا رُفض بصمت بخطأ 401:
> ```sql
> drop policy if exists "anon insert events" on public.events;
> create policy "anon insert events" on public.events for insert to anon, authenticated
>   with check (kind = any (array['visit','app_open','auth_open','signup','pay_click','paywall','download','seen','support_open','support_click']));
> ```

---

## 9. دروس مستفادة — لا تكررها

1. **الأعطال الصامتة أخطر شيء.** كل عمليات الإدراج كانت `.then(()=>{})` بلا معالجة أخطاء، فاختفت مشاكل حقيقية لأسابيع.
2. **رموز الهروب في كود مُلصَق** كسرت جسر الدفع وكلّفت أول عميل. تجنّبها.
3. **افحص قبل أن تخمّن.** «صفحة بيضاء» ظُنّت DNS وكانت إيقاف دومين · «زر مفقود» ظُنّ z-index وكان تمدداً أفقياً · «التحميلات صفر» ظُنّ خطأ وكان تصفير الشهر.
4. **الترتيب حِمل في المحللات.** رقم الرخصة `2019004455` قُرئ كجوال `(201) 900-4455` — لأن المفاتيح لم تُقنَّع قبل البحث عن الهاتف.
5. **لا تنشر بلا اختبار على مدخلات واقعية.** اختبار اللصق الذكي الإنجليزي على 6 إعلانات كشف 6 أخطاء ما كانت لتظهر بالمراجعة البصرية.
6. **علم `i` في التعبير النمطي يجعل `[A-Z]` يقبل الحروف الصغيرة** — فالتقط المحلل كلمة «at» كأنها اسم.
7. **قِس قبل أن تصدّق الرقم.** 14 تحميلاً بدت نجاحاً، وكانت كلها من حساب المالك.
8. **الرقم الصفر ليس إشارة سوق حتى تُثبت أن الطريق سالك.** «صفر تحميلات» فُسّر
   شهراً كعزوف عن المنتج، وكان زر التحميل مكسوراً على الجوال. قبل أي استنتاج
   تسويقي من رقم منخفض، **نفّذ المسار بنفسك من جهاز عميل حقيقي**.
9. **لا تجعل الخصم يسبق التسليم أبداً.** أي عدّاد حصة يُنقص قبل التأكد من وصول
   الخدمة يحوّل عطلاً تقنياً إلى شعور بالغش. الخصم يكون داخل رد نداء النجاح.
10. **حين تصلح عطلاً صامتاً لا تستبدله بصمت آخر.** أول إصلاح للتحميل جعل فشل
    التسجيل يُرجع العدّاد بلا رسالة — فاختفى العطل التالي، ولولا أن المالك
    انتبه للرقم لما اكتُشف. أظهر الخطأ دائماً.
11. **الخصائص الفيزيائية تتسرب من RTL إلى LTR.** ثلاث مرات: `clip-path` ومقياس
    `range` في المنزلق، ثم `right` في زر الدعم فغطّى لوحة التحكم كلها.
    القاعدة: في `en/` استخدم `inset-inline-start/end` و`margin-inline-*`
    و`text-align:start/end` — لا `left/right`.
12. **الصور مسألة قانونية لا جمالية فقط.** صور Zillow وRealtor محمية بحقوق
    المصور، والجمهور المستهدف (وسطاء ومصورو عقار) هو أكثر من يرفع شكاوى.
    استخدم Pexels/Unsplash أو صوراً تملكها.

---

**بالتوفيق 🚀**
