# 🏠 عقاري ديزاين / Aqari Design — ملف التسليم

> **آخر تحديث: 12 أغسطس 2026**
> مكتوب ليُفهم المشروع من الصفر بلا الرجوع إلى أي محادثة. اقرأه كاملاً قبل أي تعديل.

---

## 0. ما هو المشروع

منصة ويب تحوّل بيانات عقار + صورة إلى **منشور إنستقرام/فيسبوك احترافي في أقل من دقيقة**، بلا خبرة تصميم وبلا تثبيت أي برنامج.

تعمل بنسختين مستقلتين: **عربية** للسوق السعودي و**إنجليزية** للسوق الأمريكي.

| | |
|---|---|
| الموقع | https://aqaridesign.com · https://aqaridesign.com/en/ |
| المستودع | https://github.com/almr5-cloud/aqari-design — ⚠️ **عام/Public** |
| المجلد المحلي | `C:\Users\Admin\Desktop\برمجت موقع عقاري` |
| لوحة الإحصاءات | `https://aqaridesign.com/stats.html` + المفتاح (محفوظ عندك) |
| السعر | **$19.99/شهر** — خطة واحدة للسوقين · 30 تصميماً شهرياً |
| البريد | aqaridesign@gmail.com |
| المالك | مؤسس فرد في السعودية |

---

## 1. وين وصلنا بالضبط

### ✅ المنتج يعمل من طرف إلى طرف

يوم **12 أغسطس 2026** أُغلقت كل الأعطال التقنية المعروفة. الحلقة الكاملة مُتحقَّق منها:

| المرحلة | الحالة | كيف تُحُقِّق منها |
|---|---|---|
| يصمّم | ✅ | 6 قوالب × 3 مقاسات = **18/18** رُسمت بلا خطأ |
| يلصق إعلانه | ✅ | اللصق الذكي **6/6** على إعلانات أمريكية واقعية |
| يدفع | ✅ | نموذج Whop **مضمّن داخل الموقع**، البريد مقفل |
| يُفعَّل حسابه | ✅ | **ويبهوك مُتحقَّق منه بدفعة حقيقية من Whop** |
| يحمّل ويحفظ | ✅ | يعمل **حتى داخل متصفح إنستقرام** |
| يلغي | ✅ | `membership_deactivated` يسحب الوصول |

### 🔨 آخر ما أُنجز (بالترتيب الزمني، 12 أغسطس)

1. **إصلاح عطل التحميل** — ثلاثة أعطال متراكبة (§4 تاريخياً)
2. تسجيل التحميل: إعادة محاولة بعد تجديد الجلسة، وإظهار الخطأ بدل إخفائه
3. **تدوير `WEBHOOK_KEY`** ثم ربط ويبهوك Whop جديد — **أول استدعاء حقيقي في تاريخ المشروع**
4. **توحيد السوقين على $19.99** — 38 موضعاً في 6 ملفات
5. **الدفع المضمّن** في النسختين مع **قفل البريد**
6. تجربة زر «Whop Pay» ثم إزالته — لم يكن Apple Pay
7. **تسليم الصورة داخل متصفحات التطبيقات** ← آخر شيء خُتم به اليوم
8. تدقيق AEO ثنائي اللغة · بيت أمريكي بدل الخليجي · إصلاح تراكب قالبَي `clean` و`gallery` · `logo.png` من 502 ك.ب إلى 16

### 📊 الأرقام (12 أغسطس، بعد استبعاد الزيارات الداخلية)

| | |
|---|---|
| حسابات حقيقية | **3** (`okaoka9999m` · `salem50045004` · `salem7006`) |
| اشتراكات نشطة | **1** — `salem50045004@hotmail.com` |
| تحميلات عملاء حقيقيين | **0** |
| زيارات 7 أيام (10 أغسطس) | 191 · دخلوا المحرّر 25 · اصطدموا ببوابة الدفع 37 |

> ⚠️ **لا تقرأ «صفر تحميلات» كإشارة سوق.** العميل الوحيد حاول **15 مرة في 12 دقيقة** ولم تصله صورة — لأن زر التحميل كان مكسوراً. أُصلح وأُعيد رصيده كاملاً. الأرقام السابقة كلها قيست في زمن كان المسار فيه مكسوراً، فتلزم إعادة قياسها.

---

## 2. الملفات ودور كل ملف

### تُنشر على الموقع

| الملف | الدور |
|---|---|
| `index.html` | **صفحة الهبوط العربية** — هيرو · منزلق قبل/بعد · عرض اللصق الذكي · خطوات · قوالب · سعر · أسئلة |
| `app.html` | **المحرّر العربي** (RTL) — القوالب الستة · المحلل العربي · الحسابات · الدفع · التحميل · التتبع |
| `about.html` | صفحة محتوى عربية — **مهمة للـSEO/AEO** (يقتبس منها الذكاء الاصطناعي) |
| `en/index.html` | **صفحة الهبوط الإنجليزية** |
| `en/app.html` | **المحرّر الإنجليزي** (LTR) — نسخة مستقلة تماماً + حقول التوافق الأمريكية |
| `en/about.html` | صفحة محتوى إنجليزية (~900 كلمة) للـAEO الأمريكي |
| `stats.html` | **لوحة المالك** — محمية بمفتاح · `noindex` · محجوبة في robots |
| `demo-*.jpg` · `en/demo-*.jpg` | صور منزلق «قبل/بعد» في صفحتَي الهبوط |
| `ads/ad-1..5.jpg` | خمسة إعلانات 1080×1080 للسوق الأمريكي |
| `marketing/before-after-*.jpg` | صور «قبل/بعد» للتواصل المباشر مع الوسطاء (مربعة وعمودية) |
| `logo.png` · `og-image.png` | الشعار وصورة معاينة المشاركة |
| `tutorial.mp4` · `cancel-tutorial.mp4` | فيديو الاستخدام وفيديو الإلغاء (عربي فقط) |
| `fonts/*.woff2` | خط ثمانية — Sans للنصوص، Serif Display للأسعار (العربية فقط) |
| `robots.txt` | يسمح لزواحف الذكاء الاصطناعي · يحجب `stats.html` |
| `sitemap.xml` · `llms.txt` | خريطة الموقع للغتين + ملخص ثنائي اللغة لمحركات الإجابة |
| `CNAME` | يربط GitHub Pages بالدومين — **لا تحذفه أبداً** |

### تُنفَّذ في Supabase (ليست على الموقع)

| الملف | الدور |
|---|---|
| `supabase/functions/whop-webhook/index.ts` | **جسر الدفع** — يستقبل إشعارات Whop، يستخرج البريد، يفعّل/يلغي، ويسجّل كل استدعاء في `webhook_log` |
| `supabase-setup.sql` | جدولا `profiles` و`designs` + سياسات RLS |
| `subscriptions-setup.sql` | جدول `subscriptions` |
| `downloads-setup.sql` | جدول `downloads` (لحد الـ30 شهرياً) |
| `whop-automation.sql` | دالة `set_subscription_by_email` — للخادم فقط |
| `customers-panel.sql` | دالة `get_stats` التي تغذّي اللوحة |

### على الجهاز فقط (مستثناة من Git)

`_ads.html` (مصدر الإعلانات) · `_ba.html` (مصدر صور قبل/بعد) · `_before.html` · `_regen-after.html` · `us-house.jpg` (الصورة المصدر من Pexels) · `_lsr-logo.png` · `_prop.png` · لقطات الشاشة والتصاميم المُصدَّرة

### جداول قاعدة البيانات

`profiles` · `designs` · `subscriptions` · `downloads` · `events` · `webhook_log`

---

## 3. القرارات التقنية ولماذا

### بلا إطار عمل — كل صفحة ملف HTML واحد
HTML + CSS + JS في ملف واحد لكل صفحة. **السبب الأصلي**: الجهاز لم يكن فيه Node.js. النتيجة التي بقيت مفيدة: **نشر فوري بلا خطوة بناء**، وتحميل شبه لحظي.
> Node.js و npm **مثبتان الآن** (v24 / v11) — لكن لا تُدخل أداة بناء بلا سبب قوي.

### الرسم على Canvas في متصفح العميل
التصميم يُرسم عند الزائر لا على خادم. **السبب**: تكلفة خادم صفر (حاسم لهامش اشتراك صغير)، ومعاينة فورية، والصورة المُصدَّرة مطابقة تماماً لما يراه.

### Supabase وليس Firebase
قاعدة علائقية تناسب النموذج (مستخدم ← اشتراك ← تصاميم)، وحماية RLS على مستوى الصف، وتسعير متوقع. Firebase (NoSQL) يعقّد هذه العلاقات.

### Whop للدفع وليس Stripe أو ميسر
- **Stripe**: لا يقبل تاجراً مقيماً في السعودية بلا كيان في دولة مدعومة
- **ميسر/Paylink**: أفضل تقنياً (تدعم **مدى**) لكن تتطلب سجلاً تجارياً
- **Whop**: يقبل بائعاً فرداً بلا سجل، ويدعم الاشتراكات المتجددة، وهو **تاجر مسجّل (MoR)** يتولى الضرائب
- ⚠️ **العيب المقبول**: لا يدعم مدى · رسومه ~4-6%

### خطة واحدة للسوقين بـ$19.99
كان القرار السابق **خطتين منفصلتين** (49 ريالاً · $19) بحجة أن التوحيد يعني إما رفع سعر السعوديين أو خسارة هامش من الأمريكيين. **عكسه المالك صراحةً** بعد عرض البدائل.

**الأثر المقبول**: السوق السعودي ارتفع من 49 إلى ما يعادل ~75 ريالاً (الريال مربوط عند 3.75)، والخصم بالدولار فقد يضيف بنك العميل 2-3% رسوم تحويل.

> 📌 **سالم يبقى على خطة الريال القديمة** — Whop لا ينقل المشتركين الحاليين.

### الدفع مضمّن داخل الموقع مع قفل البريد
نموذج Whop يظهر **داخل نافذة الدفع** لا بنقل العميل إلى whop.com، مع `prefill-email` + `disable-email`.

**السبب**: كل خطوة خروج من الموقع تفقد ناساً (37 اصطدموا بالبوابة مقابل حسابين). والأهم أن قفل البريد **يغلق باب ازدواج الحسابات** الذي كسر التفعيل مع أول عميل فعلاً.

### نسختان منفصلتان لا نظام لغتين (i18n)
`app.html` و`en/app.html` **ملفان مستقلان**. **السبب**: النسخة العربية فيها عميل يدفع، وإعادة هيكلتها مخاطرة غير مبررة.
> ⚠️ **الثمن**: أي إصلاح في محرّك الرسم أو التحميل **يجب تطبيقه على الملفين**. عطل التحميل كان في الاثنين معاً.

### GitHub Pages للاستضافة
مجاني · شبكة توزيع عالمية · HTTPS تلقائي · نشر بمجرد `git push`.

### خط ثمانية محلياً وليس Google Fonts
يعمل بلا إنترنت، وهوية عربية احترافية. النسخة الإنجليزية تستخدم مكدّساً لاتينياً من النظام (لا تحمّل خطوطاً).

### لا خطة مجانية
التصميم والمعاينة مجاناً بلا تسجيل؛ الاشتراك مطلوب **للتحميل والحفظ**. قرار المالك.

---

## 4. المشاكل المفتوحة

### 🔴 لا أحد أكمل شراءً حقيقياً من البداية للنهاية — ولا مرة
لم يمرّ المالك ولا أي عميل بالمسار كاملاً في جلسة واحدة: تسجيل ← دفع ← تفعيل ← تحميل.

**لماذا هذا خطير رغم كل التحقق**: الأعطال الخمسة التي اكتُشفت يوم 12 أغسطس **لم يكشفها فحص آلي ولا مراجعة كود** — كشفها إنسان جرّب. الاختبار يثبت أن الطريق **يبدو** سالكاً؛ المشي عليه شيء آخر.

**الحل**: §5 الخطوة الأولى.

### 🟠 نزف عند التسجيل — يحتاج إعادة قياس
`191 زيارة → 41 فتحوا التسجيل → حسابان`، و**37 اصطدموا ببوابة الدفع**.
لكن هذه الأرقام قيست حين كان التحميل مكسوراً والدفع يحوّل خارج الموقع. **أعد القياس بعد أسبوعين من التسويق** قبل أي استنتاج.
**خيار مطروح (قرار المالك)**: أول تحميل مجاني بعد إنشاء حساب.

### 🟠 مفتاح الويبهوك ظهر في لقطة شاشة
دُوِّر يوم 12 أغسطس، لكن المفتاح الجديد ظهر في لقطة أثناء العمل. **دوّره مرة أخيرة** — الطريقة في §6.

### 🟠 Apple Pay لم يُتحقق من ظهوره
يتطلب جهاز Apple بـSafari وبطاقة في Wallet. وقد يتطلب **تسجيل النطاق لدى Apple** عبر Whop حين يكون الدفع مضمّناً في نطاقك.
**كيف تتحقق**: افتح `aqaridesign.com/app.html` من iPhone بـSafari واضغط الاشتراك.

### 🟠 لا يوجد Meta Pixel
بدونه لا تحسين حملات ولا retargeting. **يلزم فقط إن قررت الصرف على إعلانات مدفوعة.**

### 🟡 ملفان يجب تحديثهما معاً
أي تعديل على محرّك الرسم أو التحميل يلزم تكراره في `app.html` و`en/app.html` — ثمن قرار «نسختان لا i18n» (§3).

### 🟡 بوابة الاشتراك تُفحص في المتصفح
مبرمج متمكن يتجاوزها. مقبول للانطلاق؛ التحصين يحتاج توليد الصورة على الخادم.

### 🟡 احتمال ضئيل باقٍ لازدواج البريد
قفل البريد يغلق المسار العادي. لو حدث رغم ذلك، **الكشف فوري**: يظهر في `webhook_log` كـ`user_not_found` مع البريد، وتفعّله يدوياً في ثوانٍ (§6).
**الحل الجذري المؤجَّل**: تسجيل الدخول عبر Whop OAuth.

### 🟡 تفاصيل صغيرة
- وصف meta في الصفحات العربية 173-185 حرفاً (المثالي 110-165)
- `app.html` العربي بلا وسم `h1`
- أداة Whop CLI مثبتة وغير مسجّلة الدخول (تحتاج إعادة تشغيل لتحديث PATH)

---

## 5. الخطوة الجاية (بالترتيب)

> **الكود لم يعد القيد.** الخطوتان الأوليان ليستا برمجة، وهما الأهم.

**1️⃣ اشترِ من موقعك بنفسك مرة واحدة**
حساب جديد ببريد لم يُستخدم ← ادفع 19.99 فعلياً ← راقب: هل يظهر النموذج المضمّن ببريدك مقفلاً؟ هل يُفعَّل الحساب **بلا تحديث الصفحة**؟ هل ينزل التصميم وينقص العدّاد؟ ثم ألغِ واسترجع.
**التكلفة الصافية ~دولار واحد.** لا تبدأ التسويق قبلها.

**2️⃣ أكّد مع سالم أن التحميل صار يعمل**
أُصلح العطل وأُعيد رصيده (30 كاملة). اشتراكه على `salem50045004@hotmail.com` تحديداً — عنده حساب ثانٍ بلا اشتراك. وعُرض عليه استرجاع كامل.

**3️⃣ التسويق الأمريكي — «اشتغل قبل أن تطلب»**
اختر **مدينة واحدة** (Austin). افتح إعلان وسيط حقيقي، الصق نصه في أداتك، صمّم منشوراً، **وأرسله له هدية بلا رابط في أول رسالة**.
- ابحث في `#austinrealtor` · `#justlistedaustin` — منشورات آخر 7 أيام
- استهدف **500–5,000 متابع** (يدير حسابه بنفسه)
- **10–15 رسالة يومياً لا أكثر** × 10 أيام ← متوقع 2-5 مشتركين
> ⚠️ أنشئ حساب إنستقرام إنجليزياً وانشر فيه **4-6 منشورات قبل أي رسالة** — حساب فارغ يُقرأ كسبام ويُقيّد. الصور جاهزة في `marketing/`.

**4️⃣ أعد قياس نزف التسجيل** بعد أسبوعين من التسويق.

**5️⃣ التسويق العربي**: قروب تلقرام (1,635 عضو) وواتساب (98) — أكبر فرصة غير مستغلة.

**6️⃣ Meta Pixel** — فقط إن قررت الصرف على إعلانات مدفوعة.

**مؤجّل**: Whop OAuth · التحقق من توقيع الويبهوك · SMTP خارجي.

---

## 6. الأوامر وخطوات التشغيل

### التطوير المحلي
لا سيرفر ولا بناء — **افتح `index.html` في المتصفح مباشرة**.

### النشر (بعد أي تعديل)

```bash
cd "C:/Users/Admin/Desktop/برمجت موقع عقاري"
git add -A
git commit -m "وصف التعديل"
git push
git branch -f gh-pages main
git push origin gh-pages -f
```

> ⚠️ **الموقع يُنشر من فرع `gh-pages`** — لا تنسَ مزامنته، وإلا لن يظهر التعديل.

**متابعة النشر** (1-3 دقائق):

```bash
gh api repos/almr5-cloud/aqari-design/pages/builds/latest --jq '.status + " " + .commit[0:7]'
```

قارن الـcommit بـ`git rev-parse --short HEAD`.

### قراءة الإحصاءات من الطرفية

```bash
curl -s -X POST "https://pjhafrhjmvyirrcmnrhc.supabase.co/rest/v1/rpc/get_stats" -H "apikey: <PUBLISHABLE_KEY>" -H "Content-Type: application/json" -d '{"p_key":"<STATS_KEY>"}'
```

### استبعاد زياراتك من الإحصاءات
افتح مرة واحدة على **كل جهاز**: `https://aqaridesign.com/?internal=1`
للإلغاء `?internal=0`. تحقق عبر `internal_excluded_7d`.

### تفعيل اشتراك يدوياً (لو فشل التلقائي)

```bash
curl -X POST "https://pjhafrhjmvyirrcmnrhc.supabase.co/functions/v1/whop-webhook?key=<WEBHOOK_KEY>" -H "Content-Type: application/json" -d '{"action":"membership_activated","data":{"user":{"email":"بريد_العميل"}}}'
```

الرد المتوقع `OK: activated:<uuid>` · و`user_not_found` يعني البريد غير مسجّل.

### اختبار الجسر بلا التأثير على أحد

```bash
curl -X POST "https://pjhafrhjmvyirrcmnrhc.supabase.co/functions/v1/whop-webhook?key=<WEBHOOK_KEY>" -H "Content-Type: application/json" -d '{"action":"payment_succeeded","data":{"user":{"email":"test@example.com"}}}'
```

بريد غير مسجّل ⇒ `user_not_found` ⇒ لا يتغيّر شيء.

### تدوير مفتاح الويبهوك

**١.** ولّد مفتاحاً **واحفظه في ملف عندك أولاً**:
```bash
node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
```
**٢.** Supabase ← Edge Functions ← **Secrets** ← الاسم `WEBHOOK_KEY` والقيمة ← **Save** ← ثم **Replace secret** في نافذة التأكيد.
**٣.** حدّث الرابط في Whop ← Developer ← Webhooks ← Edit.
**٤.** تحقق: الصق الرابط في المتصفح — `unauthorized` تعني أنه لم يُحفظ.

> ⚠️ **Supabase لا يعرض قيم الأسرار أبداً** (بصمة SHA256 فقط). لو أغلقت الصفحة قبل نسخ المفتاح فقد ضاع.
> 🐞 **فخ**: زر Save يفتح نافذة تأكيد شفافة تبتلع النقرات التالية. **انظر إلى الشاشة** بعد الضغط.

### إعدادات ويبهوك Whop

**الأحداث الأربعة**: `payment_succeeded` · `membership_activated` · `membership_deactivated` · `invoice_paid`

> ⚠️ **لا تضف** `membership_created` (تُنشأ العضوية أحياناً قبل الدفع) · ولا `payment_failed` (يطرد عميلاً ستنجح إعادة محاولته) · ولا `membership_cancel_at_period_end_changed` (يقطع خدمةً دُفع ثمنها).
> 📌 Whop يرسل الأحداث **بنقطة** (`payment.succeeded`) رغم أن واجهته تعرضها بشرطة سفلية. الجسر يقبل الصيغتين — لا تستبدله بمطابقة حرفية.

### تعديل قاعدة البيانات
Supabase ← SQL Editor ← الصق ملف `.sql` ← Run.
> 🐞 المحرّر لا يُحمَّل إذا كان تبويب كروم **مخفياً أو في الخلفية**.
> 🐞 اكتب SQL **في سطر واحد** — المحاذاة التلقائية تفسد الأسطر المتعددة.

### تعديل جسر Whop
Supabase ← Edge Functions ← `whop-webhook` ← Code ← عدّل ← **Deploy updates**.
> ⚠️ **لا تستخدم رموز الهروب** (`\w`, `\.`) في التعبيرات النمطية هنا. لصقها عبر أدوات وسيطة كسرها مرة، فتوقف استخراج البريد وأُهملت كل الإشعارات بصمت — وكلّف ذلك أول عميل. استخدم `[A-Za-z0-9]` و`[.]`.
> ⚠️ **حدّث الملف في المستودع في نفس اليوم**.

### الدومين
Namecheap ← Advanced DNS:

```
A     @    185.199.108.153
A     @    185.199.109.153
A     @    185.199.110.153
A     @    185.199.111.153
CNAME www  almr5-cloud.github.io
```

**ينتهي 18 يوليو 2027** — فعّل التجديد التلقائي.
> 🐞 ظهرت «صفحة بيضاء» مرة بسبب **إيقاف الدومين لعدم تأكيد بريد التسجيل** (متطلب ICANN)، لا بسبب DNS.

### إعادة توليد الإعلانات وصور العرض

```bash
msedge --headless=new --window-size=1080,1080 --screenshot="ads/ad-1.png" --allow-file-access-from-files --virtual-time-budget=6000 "_ads.html?s=1"
```

(مسار Edge: `C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe`)

> 🐞 **HTML الثابت يعمل في Edge بلا واجهة. صفحات الكانفس لا.**
> لتوليد صورة من محرّك الرسم: انسخ المحرّر مع الصور مدمجة كـ`data:`، استدعِ `render()` فوراً وقت التحليل (لا تنتظر `load`)، ثم `cv.toDataURL` واقرأ الناتج بـ`--dump-dom`. **لا تلتقط لقطة شاشة** — الكانفس يُعرض بارتفاع مختلف فينتج شريط أسود وقصّ.

---

## 7. المفاتيح والأسرار

> ⚠️ **لا تكتب أي سر في هذا المستودع — هو عام.** ولا تلصقه في محادثة.

| السر | مكانه |
|---|---|
| Supabase URL + publishable key | مكتوبان في صفحات الموقع — **آمنان بالتصميم** (الحماية بـRLS) |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase ← Settings ← API — سري |
| `WEBHOOK_KEY` | Supabase ← Edge Functions ← **Secrets** |
| `WHOP_WEBHOOK_SECRET` | نفس المكان — محفوظ للمستقبل، **غير مستخدم اليوم** (الحماية بمفتاح الرابط) |
| مفتاح لوحة الإحصاءات | داخل دالة `get_stats` + في متصفحك |

**معرّف مشروع Supabase**: `pjhafrhjmvyirrcmnrhc`

**تدوير مفتاح اللوحة** (سطر واحد في SQL Editor):

```sql
do $$ declare src text; begin select pg_get_functiondef(p.oid) into src from pg_proc p join pg_namespace n on n.oid = p.pronamespace where n.nspname='public' and p.proname='get_stats'; src := replace(src,'القديم','الجديد'); execute src; end $$;
```

---

## 8. ثوابت مهمة في الكود

| الثابت | القيمة (النسختان معاً) |
|---|---|
| `PAYMENT_PLAN_ID` | `plan_fa9qheRRrrnUO` — خطة واحدة بـ$19.99 |
| `PAYMENT_LINK` | مُشتق من المعرّف — احتياطي لو حجب مانع الإعلانات السكربت |
| `PRO_MONTHLY_DOWNLOADS` | `30` |
| `SUPPORT_EMAIL` | aqaridesign@gmail.com |

**السعر مكتوب في 38 موضعاً** موزّعة على `index.html` · `app.html` · `about.html` · `en/index.html` · `en/app.html` · `en/about.html` · `llms.txt` و`_ads.html` (ثم يلزم إعادة توليد `ads/ad-5.jpg`).
> ⚠️ لا تغيّره يدوياً موضعاً موضعاً — اكتب سكربت استبدال يتحقق من **عدد المرات المتوقّع لكل نص**، ثم افحص أن صفر بقايا للسعر القديم. وراجع تكلفة التصميم الواحد (`السعر ÷ 30`) في بطاقة المقارنة.

**خصائص الدفع المضمّن** (مستخرجة من `js.whop.com/static/checkout/index.js` نفسه، لا مخمّنة):
`data-whop-checkout-plan-id` · `prefill-email` · `disable-email` · `theme` · `theme-accent-color` · `skip-redirect`

**أحداث التتبع المسموحة**: `visit` · `app_open` · `seen` · `auth_open` · `signup` · `pay_click` · `paywall` · `download` · `support_open` · `support_click`

> ⚠️ لإضافة نوع حدث جديد **يجب** تحديث سياسة RLS واسمها `anon insert events`، وإلا رُفض بصمت بخطأ 401:
> ```sql
> drop policy if exists "anon insert events" on public.events;
> create policy "anon insert events" on public.events for insert to anon, authenticated with check (kind = any (array['visit','app_open','auth_open','signup','pay_click','paywall','download','seen','support_open','support_click']));
> ```

---

## 9. دروس مستفادة — لا تكررها

1. **الأعطال الصامتة أخطر شيء.** كل عمليات الإدراج كانت `.then(()=>{})` بلا معالجة أخطاء، فاختفت مشاكل حقيقية لأسابيع.
2. **رموز الهروب في كود مُلصَق** كسرت جسر الدفع وكلّفت أول عميل.
3. **افحص قبل أن تخمّن.** «صفحة بيضاء» ظُنّت DNS وكانت إيقاف دومين · «زر مفقود» ظُنّ z-index وكان تمدداً أفقياً.
4. **الترتيب حِمل في المحللات.** رقم رخصة `2019004455` قُرئ كجوال `(201) 900-4455` — لأن المفاتيح لم تُقنَّع قبل البحث عن الهاتف.
5. **لا تنشر بلا اختبار على مدخلات واقعية.** اختبار اللصق الذكي على 6 إعلانات كشف 6 أخطاء ما كانت لتظهر بالمراجعة البصرية.
6. **علم `i` في التعبير النمطي يجعل `[A-Z]` يقبل الحروف الصغيرة** — فالتقط المحلل كلمة «at» كأنها اسم.
7. **قِس قبل أن تصدّق الرقم.** 14 تحميلاً بدت نجاحاً، وكانت كلها من حساب المالك.
8. **الرقم الصفر ليس إشارة سوق حتى تُثبت أن الطريق سالك.** «صفر تحميلات» فُسّر شهراً كعزوف عن المنتج، وكان زر التحميل مكسوراً على الجوال.
9. **لا تجعل الخصم يسبق التسليم أبداً.** أي عدّاد حصة يُنقص قبل التأكد من وصول الخدمة يحوّل عطلاً تقنياً إلى شعور بالغش.
10. **حين تصلح عطلاً صامتاً لا تستبدله بصمت آخر.** أول إصلاح للتحميل جعل فشل التسجيل يُرجع العدّاد بلا رسالة، فاختفى العطل التالي.
11. **الخصائص الفيزيائية تتسرب من RTL إلى LTR.** ثلاث مرات: `clip-path` ومقياس `range` في المنزلق، ثم `right` في زر الدعم فغطّى لوحة التحكم. في `en/` استخدم `inset-inline-start/end` و`margin-inline-*` و`text-align:start/end`.
12. **الصور مسألة قانونية لا جمالية.** صور Zillow وRealtor محمية بحقوق المصور، والجمهور المستهدف هو أكثر من يرفع شكاوى. استخدم Pexels/Unsplash أو صوراً تملكها.
13. **استخرج أسماء خصائص المزوّد من سكربته، لا من كلام مساعده.** مساعد Whop أعطى أسماء أحداث بنقطة بينما واجهته تعرضها بشرطة، وأعطى `return-url` لصفحة `/checkout/complete` **غير موجودة** كانت سترمي كل عميل دفع بنجاح على صفحة 404.
14. **انظر إلى الشاشة قبل أن تفحص برمجياً.** «زر Save لا يعمل» في Supabase كان نافذة تأكيد فُتحت بنجاح، وكل نقرة تالية ترتطم بخلفيتها.
15. **الأسرار التي لا تُقرأ مرتين تُنسخ أولاً.** اكتب السر في ملف عندك **قبل** حفظه في أي لوحة.
16. **«يبدو أنه يعمل» ليس «يعمل».** رابط `blob:` الاحتياطي بدا حلاً لمشكلة التحميل داخل إنستقرام، وكان يعرض الصورة **كمستند** فلا تُحفظ. ولم يُكتشف إلا حين جرّبه إنسان على جهازه.

---

## 🔑 الخلاصة في سطرين

المنتج **جاهز تقنياً ومُتحقَّق من كل حلقة فيه**. القيد الوحيد الباقي أن أحداً لا يعرف المنصة.

**افعل §5 بالترتيب. لا تبنِ ميزة جديدة قبل أول ثلاثة عملاء.**

**بالتوفيق 🚀**
