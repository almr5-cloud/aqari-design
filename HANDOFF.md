# 🏠 عقاري ديزاين — ملف التسليم (HANDOFF)

> آخر تحديث: **7 أغسطس 2026**
> هذا الملف يشرح المشروع من الصفر. اقرأه كاملاً قبل أي تعديل.

---

## 1. نظرة عامة

**عقاري ديزاين** منصة SaaS سعودية تتيح للوسطاء والمكاتب العقارية تصميم منشورات إنستقرام لعقاراتهم في أقل من دقيقة، بدون خبرة تصميم.

| | |
|---|---|
| **الموقع الحي** | https://aqaridesign.com |
| **المستودع** | https://github.com/almr5-cloud/aqari-design (⚠️ **عام/Public**) |
| **المجلد المحلي** | `C:\Users\Admin\Desktop\برمجت موقع عقاري` |
| **لوحة الإحصاءات** | `https://aqaridesign.com/stats.html?key=<STATS_KEY>` |
| **السعر** | 49 ريال/شهر · 30 تصميماً شهرياً |
| **البريد الرسمي** | aqaridesign@gmail.com |

**الميزة المميّزة (الحصرية):** «اللصق الذكي» — الوسيط يلصق نص إعلانه من واتساب، فتُستخرج تلقائياً: نوع العقار، السعر، الموقع، الغرف، دورات المياه، المساحة، الجوال، واسم المكتب. يفهم صيغاً عربية متنوعة (فيلا/فله، حمامات/دورات مياه، 570ك/570 ألف/1.2 مليون، الأرقام العربية ٠-٩، جوال بصيغ 05/+966/966).

---

## 2. وين وصلنا بالضبط (الحالة الحالية)

### ✅ مكتمل ويعمل
- **التطبيق**: 6 قوالب (عصري · فاخر · نظيف · بطاقة · معرض · شريطي) × 3 مقاسات (1080×1080، 1080×1350، 1080×1920)
- **اللصق الذكي** + حقول إضافية ديناميكية (زر ➕ للمعلومات والمواصفات)
- **الحسابات**: تسجيل/دخول فوري (تأكيد البريد **معطّل** عمداً)، نسيت كلمة المرور
- **الاشتراكات**: بوابة دفع، حد 30 تحميلاً شهرياً، عدّاد المتبقي
- **الدفع**: Whop 49 ريال شهري متجدد + تفعيل تلقائي عبر Webhook
- **الحفظ السحابي**: حفظ التصاميم واسترجاعها
- **خدمة العملاء**: زر عائم 💬 + نافذة بأسباب جاهزة ترسل بريداً يحمل بيانات حساب العميل تلقائياً
- **لوحة إحصاءات خاصة**: مسار العميل، الأجهزة، المصادر، **قسم العملاء ونشاطهم**، فحص SEO/AEO مباشر
- **SEO/AEO كامل**: 10/10 و 7/7 — llms.txt، FAQ schema، robots للزواحف الذكية، Search Console
- **الدومين**: aqaridesign.com + HTTPS + www→root

### 📊 الأرقام (7 أغسطس 2026)
- **أول عميل دافع**: `salem50045004@hotmail.com` — 49 ريال، مشترك نشط ✅
- الحسابات المسجّلة: 5 (منها 2 اختبارية)
- الاشتراكات النشطة: 2 (العميل + حساب المالك)
- الزيارات: ~79 أسبوعياً
- التواصل التسويقي: ~60 وسيط عبر واتساب

### 🔨 آخر شي خلصناه
1. إصلاح **جسر Whop** — كان فيه خطأ في تعبير استخراج البريد (رموز هروب مكسورة: `\\w` بدل `\w`) فكانت كل الإشعارات تُهمل بصمت. أُعيد بلا رموز هروب + توسيع الأحداث + تسجيل كل إشعار في `webhook_log`
2. **«آخر دخول» يتحدث تلقائياً** — كان يعتمد على تسجيل الدخول بكلمة المرور فقط
3. **إصلاح زر المساعدة على الجوال** — السبب كان تمدد الصفحة أفقياً (الشريط العلوي أعرض من الشاشة) فيدفع الزر خارجها

---

## 3. القرارات التقنية ولماذا

### لا إطار عمل (No framework) — ملف HTML واحد
`index.html` يحتوي كل شي (HTML + CSS + JS). **السبب**: جهاز المالك لا يحتوي Node.js ولا Python، فأي أداة بناء مستحيلة. النتيجة: نشر فوري بلا خطوة بناء، وسرعة تحميل 0.12 ثانية.

### الرسم على Canvas (وليس صور من السيرفر)
التصميم يُرسم في متصفح العميل. **السبب**: تكلفة صفر على السيرفر (مهم لهامش 49 ريال)، ومعاينة فورية.

### Supabase (وليس Firebase)
**السبب**: قاعدة بيانات علائقية (PostgreSQL) تناسب نموذج SaaS (مستخدم ← اشتراك ← تصاميم)، حماية RLS على مستوى الصف، وتسعير متوقع. Firebase (NoSQL) يعقّد العلاقات.

### Whop للدفع (وليس Stripe أو ميسر)
- **Stripe**: مرفوض — لا يقبل تجار مقيمين في السعودية (يتطلب كياناً بدولة مدعومة)
- **ميسر/Paylink**: أفضل تقنياً (تدعم **مدى**) لكن تتطلب سجلاً تجارياً أو وثيقة عمل حر
- **Whop**: يقبل بائعاً فرداً من السعودية بلا سجل تجاري، ويدعم الاشتراكات المتجددة
- ⚠️ **العيب المقبول**: لا يدعم مدى (فيزا/ماستركارد/Apple Pay فقط)، ورسومه ~4-6%

### GitHub Pages للاستضافة
**السبب**: مجاني، شبكة توزيع عالمية (لا يسقط)، HTTPS تلقائي، ونشر بمجرد `git push`.

### خط ثمانية محلياً (وليس Google Fonts)
ملفات woff2 في `fonts/`. **السبب**: يعمل بلا إنترنت، وهوية عربية احترافية.

### نموذج الاشتراك: لا خطة مجانية
التصميم والمعاينة مجاناً، والاشتراك مطلوب **للتحميل والحفظ**. قرار المالك بعد استبعاد التجربة المجانية.

---

## 4. الملفات ودور كل ملف

### الأساسية (تُنشر على الموقع)
| الملف | الدور |
|---|---|
| `index.html` | **صفحة الهبوط** (الصفحة الرئيسية) — تبيع المنتج قبل ما يُطلب التسجيل: هيرو، منزلق قبل/بعد، عرض اللصق الذكي، الخطوات، القوالب، السعر، أسئلة شائعة. بلا مكتبات خارجية |
| `app.html` | **التطبيق كامل** — الواجهة، القوالب الستة، اللصق الذكي، الحسابات، الاشتراك، خدمة العملاء، التتبع |
| `demo-before.jpg` · `demo-after.jpg` | صور منزلق «قبل/بعد» في صفحة الهبوط (مصغّرة بـffmpeg من 1.8MB إلى 107KB) |
| `stats.html` | لوحة الإحصاءات الخاصة بالمالك (محمية بمفتاح) — مسار العميل، العملاء، فحص SEO/AEO |
| `en/index.html` | **صفحة الهبوط الإنجليزية** للسوق الأمريكي |
| `en/app.html` | **المحرّر بالإنجليزية** (LTR) — نسخة مستقلة عن العربية |
| `en/demo-*.jpg` | صور «قبل/بعد» الإنجليزية |
| `about.html` | صفحة محتوى نصية غنية — **مهمة للـSEO/AEO** (يقتبس منها الذكاء الاصطناعي) |
| `logo.png` | الشعار (512×512) |
| `og-image.png` | صورة معاينة المشاركة في واتساب/تويتر (1200×630) |
| `tutorial.mp4` | فيديو تعليمي (داخل نافذة «كيف أستخدم؟») |
| `cancel-tutorial.mp4` | فيديو طريقة الإلغاء (رابط هادئ مخفي) |
| `fonts/*.woff2` | خط ثمانية (Sans للنصوص + Serif Display للأسعار) |
| `robots.txt` | يسمح لزواحف الذكاء الاصطناعي، ويحجب `stats.html` |
| `sitemap.xml` · `llms.txt` | خريطة الموقع + ملخص منظّم لنماذج الذكاء الاصطناعي |
| `CNAME` | يربط GitHub Pages بالدومين — **لا تحذفه** |

### السحابية (تُنفَّذ في Supabase، ليست على الموقع)
| الملف | الدور |
|---|---|
| `supabase/functions/whop-webhook/index.ts` | **الجسر**: يستقبل إشعارات Whop ويفعّل/يلغي الاشتراك تلقائياً + يسجّل كل إشعار |
| `supabase-setup.sql` | الجداول الأساسية: `profiles` · `designs` + سياسات RLS |
| `subscriptions-setup.sql` | جدول `subscriptions` (قراءة فقط للمستخدم) |
| `downloads-setup.sql` | جدول `downloads` (لحد الـ30 شهرياً) |
| `whop-automation.sql` | دالة `set_subscription_by_email` (للخادم فقط) |
| `customers-panel.sql` | دالة `get_stats` — تغذّي لوحة الإحصاءات |

### مواد التسويق (على الجهاز فقط — مستثناة من Git)
`كاروسيل/slide-1..7.png` (كاروسيل إنستقرام/تيك توك) · `عقاري-ديزاين-قبل-وبعد.pdf` · `before-design.png` · `after-design.png` · `_carousel.html` · `_brochure.html` (مصادر قابلة للتعديل وإعادة التوليد)

---

## 5. المفاتيح والأسرار — وين تجدها

> ⚠️ **لا تكتب أي سر في هذا المستودع — هو عام.**

| السر | مكانه |
|---|---|
| Supabase URL + publishable key | مكتوبان في `index.html` و`stats.html` — **آمنان** (مصممان للعلن، والحماية بـRLS) |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase → Settings → API — **سري، لا يُكتب في أي ملف** |
| `WEBHOOK_KEY` (مفتاح جسر Whop) | Supabase → Edge Functions → **Secrets** (متغير بيئة، ليس في الكود) |
| مفتاح لوحة الإحصاءات | داخل دالة `get_stats` في قاعدة البيانات + في رابط اللوحة الذي تحفظه أنت |

**معرّف مشروع Supabase**: `pjhafrhjmvyirrcmnrhc`

---

## 6. المشاكل المفتوحة (لم تُحل)

### ✅ مُغلَقة — مفتاح لوحة الإحصاءات (كان مكشوفاً)
كان مكتوباً نصاً في `customers-panel.sql` داخل مستودع عام. **حُلّت في 8 أغسطس 2026**: عُقّم الملف، ودُوِّر المفتاح في دالة `get_stats`. المفتاح القديم الموجود في تاريخ Git **لم يعد يعمل** (تحقّقنا: يرجع `unauthorized`).
> 💡 المفتاح **غير مكتوب** في `stats.html` — تُدخله يدوياً ويُحفظ في متصفحك. فأي تدوير مستقبلي = تعديل الدالة فقط، بلا نشر.
> **طريقة التدوير** (Supabase → SQL Editor، سطر واحد):
> ```sql
> do $$ declare src text; begin select pg_get_functiondef(p.oid) into src from pg_proc p join pg_namespace n on n.oid = p.pronamespace where n.nspname = 'public' and p.proname = 'get_stats'; src := replace(src, 'المفتاح_القديم', 'المفتاح_الجديد'); execute src; end $$;
> ```

### 🟠 متوسطة — لم نتحقق من حمولة Whop الحقيقية
الجسر مُصلَح ومختبر بـ7 حالات مصنوعة، **لكن لم نرَ إشعاراً حقيقياً من Whop بعد الإصلاح**. الاحتمال المتبقي: حمولة Whop قد لا تحتوي بريد العميل (معرّفات فقط) — وحينها يحتاج استدعاء واجهة Whop لجلب البريد.
**كيف نتحقق**: أول دفعة جديدة → اقرأ جدول `webhook_log` (يحفظ الحمولة كاملة).
**احتياطياً**: راقب Whop، وأي دفعة لا تُفعّل فعّلها يدوياً (انظر §8).

### 🟠 متوسطة — ازدواج البريد بين الموقع و Whop
العميل قد يسجّل ببريد ويدفع ببريد آخر (شائع مع Apple Pay) فلا يُفعّل اشتراكه. حالياً نُخفّفه بنافذة تحذير قبل الدفع.
**الحل الجذري المقترح**: تسجيل الدخول عبر Whop OAuth — يلغي الازدواج نهائياً.

### 🟡 منخفضة — بوابة الاشتراك تُفحص في المتصفح
مبرمج متمكن يستطيع تجاوزها والتحميل بلا دفع. مقبول للانطلاق؛ التحصين يحتاج توليد الصورة على الخادم.

### 🟡 العميل الأول لم يستخدم المنتج
سالم دفع (5 أغسطس) ولم يحمّل أي تصميم. آخر دخول له 5 أغسطس. أُرسلت له رسالتان دون تفاعل.

### 🟡 حد الـ30 تحميلاً يُحسب بالمتصفح والقاعدة معاً
العدّاد يقرأ من `downloads`، لكن الفحص يقع في المتصفح — نفس ملاحظة البوابة أعلاه.

---

## 7. الخطوة الجاية

**الأولوية 0 — إنشاء خطة USD في Whop** 💵: `en/app.html` فيه `PAYMENT_LINK = ''` فارغ عمداً. أنشئ خطة **19 دولار/شهر** في Whop والصق رابط الدفع فيه، وإلا لن يستطيع أي وسيط أمريكي الاشتراك.

**الأولوية 1 — إصلاح نزف التسجيل** 🩹: في آخر 7 أيام: **131 زيارة → 32 فتحوا التسجيل → حسابان فقط**. أي **94% ينسحبون عند شاشة التسجيل**. هذي أكبر نقطة نزف في المنتج، وأهم من أي قالب جديد.

**الأولوية 2 — تحقق**: عند أول دفعة جديدة، اقرأ `webhook_log` وتأكد أن التفعيل التلقائي عمل فعلاً.

**الأولوية 3 — تسويق (الأهم للنمو)**:
- 🎉 **البحث بدأ يعمل**: 33 زيارة من google.com في 30 يوماً — مصدر مجاني ينمو من نفسه
- المشكلة **ليست** المنتج ولا السعر — بل **قمة المسار**: 60 وسيط → عميل واحد
- **قروب تلقرام (1,635 عضو) وواتساب (98)** — أكبر فرصة غير مستغلة. انشر الكاروسيل فيهما
- واصل 10 رسائل يومياً (5 وسطاء + 5 مكاتب)، **وتابع مرتين** لمن لم يرد
- تابع العميل الأول بمكالمة أو بتصميم مجاني لعقاره — رده يعلّمك أكثر من 60 رسالة

**تحسينات مؤجلة**: تسجيل دخول عبر Whop · ربط SMTP خارجي (بريد Supabase محدود جداً) · تتبّع زوار فريدين · قوالب إضافية.

---

## 8. أوامر وخطوات التشغيل

### التطوير المحلي
لا يوجد سيرفر ولا خطوة بناء — **افتح `index.html` في المتصفح مباشرة**.
(بعض الميزات مثل روابط البريد تعمل أفضل على الموقع الحي)

### النشر (كل تحديث)
```bash
cd "C:/Users/Admin/Desktop/برمجت موقع عقاري"
git add -A
git commit -m "وصف التعديل"
git push
git branch -f gh-pages main
git push origin gh-pages -f
```
> ⚠️ **مهم**: الموقع يُنشر من فرع **`gh-pages`** — لا تنسَ مزامنته بعد كل push على `main`، وإلا لن يظهر التعديل.

**تحفيز بناء يدوياً** (لو تعطّل النشر):
```bash
gh api -X POST repos/almr5-cloud/aqari-design/pages/builds
```
**فحص حالة النشر**:
```bash
gh api repos/almr5-cloud/aqari-design/pages/builds/latest --jq '.status'
```

### تفعيل اشتراك عميل يدوياً (لو فشل التفعيل التلقائي)
```bash
curl -X POST "https://pjhafrhjmvyirrcmnrhc.supabase.co/functions/v1/whop-webhook?key=<WEBHOOK_KEY>" \
  -H "Content-Type: application/json" \
  -d '{"action":"membership_activated","data":{"id":"manual","user_email":"بريد_العميل"}}'
```
الرد المتوقع: `OK: activated:<uuid>` · إن ظهر `user_not_found` فالبريد غير مسجّل في الموقع.

### قراءة الإحصاءات من الطرفية (بدون اللوحة)
```bash
curl -s -X POST "https://pjhafrhjmvyirrcmnrhc.supabase.co/rest/v1/rpc/get_stats" \
  -H "apikey: <PUBLISHABLE_KEY>" -H "Content-Type: application/json" \
  -d '{"p_key":"<STATS_KEY>"}'
```

### تعديل قاعدة البيانات
Supabase → SQL Editor → الصق ملف `.sql` → Run.
> 🐞 **مشكلة معروفة**: محرّر SQL لا يُحمَّل إذا كان تبويب المتصفح **مخفياً/في الخلفية** (كروم يوقف رسم المكونات الثقيلة). **الحل**: اجعل نافذة كروم ظاهرة أمامك.

### تعديل جسر Whop
Supabase → Edge Functions → `whop-webhook` → Code → عدّل → **Deploy updates** → أكّد.
> ⚠️ احذر رموز الهروب (`\`) عند لصق كود يحتوي تعبيرات نمطية — هذا سبب العطل السابق. تجنّبها (استخدم `[A-Za-z0-9]` بدل `\w`، و`[.]` بدل `\.`).

### الدومين
Namecheap → Advanced DNS. السجلات المطلوبة:
```
A     @    185.199.108.153
A     @    185.199.109.153
A     @    185.199.110.153
A     @    185.199.111.153
CNAME www  almr5-cloud.github.io
```
**ينتهي الدومين: 18 يوليو 2027** — فعّل التجديد التلقائي.
> 🐞 **تجربة سابقة**: ظهرت «صفحة بيضاء» بسبب **إيقاف الدومين لعدم تأكيد بريد التسجيل** (متطلب ICANN) — لا علاقة لها بـDNS. راقب بريد Namecheap.

### إعادة توليد مواد التسويق
```bash
# الكاروسيل (7 شرائح)
msedge --headless=new --window-size=1080,1350 --screenshot="كاروسيل/slide-1.png" --allow-file-access-from-files "_carousel.html?s=1"
# الكتيّب PDF
msedge --headless=new --no-pdf-header-footer --print-to-pdf="عقاري-ديزاين-قبل-وبعد.pdf" --allow-file-access-from-files "_brochure.html"
```
(مسار Edge: `C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe`)

---

## 9. ثوابت مهمة في الكود (`index.html`)

| الثابت | القيمة الحالية | الوظيفة |
|---|---|---|
| `PAYMENT_LINK` | رابط Whop للسداد | صفحة الدفع |
| `PRO_MONTHLY_DOWNLOADS` | `30` | حد التحميل الشهري — غيّره من هنا فقط |
| `SUPPORT_EMAIL` | `aqaridesign@gmail.com` | بريد خدمة العملاء |
| `SUPABASE_URL` / `SUPABASE_ANON_KEY` | مشروع Supabase | الاتصال بالسحابة |

**جداول قاعدة البيانات**: `profiles` · `designs` · `subscriptions` · `downloads` · `events` · `webhook_log`
**أحداث التتبع المسموحة**: `visit` (صفحة الهبوط) · `app_open` (دخول المحرّر) · `seen` · `auth_open` · `signup` · `pay_click` · `paywall` · `download` · `support_open` · `support_click`
> لإضافة نوع حدث جديد **يجب** تحديث سياسة RLS على جدول `events` (اسمها `anon insert events`)، وإلا يُرفض بصمت بخطأ 401.
> ```sql
> drop policy if exists "anon insert events" on public.events;
> create policy "anon insert events" on public.events for insert to anon, authenticated
>   with check (kind = any (array['visit','app_open','auth_open','signup','pay_click','paywall','download','seen','support_open','support_click']));
> ```
> ⚠️ **لوحة الإحصاءات لا تعرض `app_open` بعد** — دالة `get_stats` تحتاج تحديثاً ليظهر في المسار: زيارة ← دخول المحرّر ← تسجيل.

---

## 10. دروس مستفادة (تجنّب تكرار الأخطاء)

1. **الأعطال الصامتة أخطر شي** — كل عمليات الإدراج كانت `.then(() => {})` بلا معالجة أخطاء، فاختفت مشاكل حقيقية. الآن نُظهر الأخطاء.
2. **رموز الهروب في الكود المُلصَق** كسرت جسر الدفع لأيام. تجنّب `\` في التعبيرات النمطية.
3. **افحص قبل أن تخمّن** — «صفحة بيضاء» ظننا أنها DNS وكانت إيقاف دومين؛ «زر مفقود» ظننا z-index وكان تمدداً أفقياً.
4. **تجربة المستخدم على الجوال تختلف** — اختبر على عرض 375px فعلياً.
5. **تطابق التسويق مع المنتج** — كان الإعلان يقول «بلا حدود» والموقع «30 شهرياً». صُحّح.

---

**بالتوفيق 🚀**


---

## 11. النسخة الإنجليزية (السوق الأمريكي) — أُطلقت 9 أغسطس 2026

**البنية**: `aqaridesign.com/en/` هبوط · `aqaridesign.com/en/app.html` محرّر. مرتبطة بالعربية عبر `hreflang` وزر تبديل لغة في الشريطين.

### قرار معماري: نسختان منفصلتان لا نظام لغتين
`app.html` و`en/app.html` **ملفان مستقلان**. السبب: النسخة العربية فيها عميل يدفع، وإعادة هيكلتها لدعم i18n مخاطرة غير مبررة.
> ⚠️ **الثمن**: أي إصلاح في المحرّك (قالب، خلل رسم) **يجب تطبيقه على الملفين**. راجع هذا القرار لو تعدّدت الأسواق.

### فروق جوهرية عن النسخة العربية
| | العربية | الإنجليزية |
|---|---|---|
| الاتجاه | RTL · خط ثمانية | LTR · مكدّس لاتيني |
| اللصق الذكي | ✅ محلل عربي | ❌ **محذوف** — يحتاج محلل إنجليزي (bd/ba/sqft/MLS#/$) |
| السعر | `2,500,000 ريال` | `$875,000` (الرمز قبل الرقم — دالة `money()`) |
| ترتيب المواصفات | — | `Beds / Baths / Sq Ft` (عُكست في 4 قوالب ترسم من اليمين) |
| حقول التوافق | — | ✅ Brokerage · License # · شعار EHO |
| السعر | 49 ريال | 19 دولار |

### شريط التوافق الأمريكي
`drawCompliance()` يرسم شريطاً سفلياً على **القوالب الستة** فيه شعار Equal Housing Opportunity واسم المكتب ورقم الرخصة. لا يظهر إلا إذا مُلئ أحد الحقول. الأهم: `complianceH()` **تُقلّص ارتفاع الرسم** فلا يغطي الشريط سطر الوسيط.

### قياس القمع الأمريكي
زيارات `/en` تُسجَّل بمصدر **`en`** تلقائياً — يظهر في «مصادر الزيارات» بلوحتك، فتفصل السوقين بلا أي تعديل على قاعدة البيانات.

### توليد صور العرض (طريقة مجرّبة)
Edge headless **لا ينفّذ حدث `load`** قبل اللقطة، فلا يصلح لأي صفحة تعتمد canvas. الطريقة الناجحة:
1. نسخة من المحرّر مع الصور مدمجة كـ`data:` وسكربت يملأ الحقول ويرسم
2. فتحها في متصفح حقيقي وتصدير `canvas.toDataURL`
3. إرسالها إلى خادم بايثون محلي على `127.0.0.1:8765` يكتب الملف
> HTML الثابت (بلا canvas) يعمل في Edge headless بلا مشاكل — استُخدم لصور «قبل».

### ما لم يُنجز بعد
- ❌ **خطة الدفع بالدولار** (أولوية 0)
- ❌ محلل لصق ذكي إنجليزي — الميزة المميزة غائبة عن النسخة الإنجليزية
- ❌ `about.html` إنجليزية (تدقيق AEO يفحص العربية فقط)
- ❌ فيديو تعليمي إنجليزي (أُزيل من نافذة الشرح)
- ❌ صفر توزيع في أمريكا — لا شبكة ولا مصداقية محلية بعد


---

## 12. أداة Whop في الطرفية + الإعلانات (10 أغسطس 2026)

### مثبّتة وجاهزة
```bash
npm install -g @whop/cli      # الحزمة الرسمية — whopio/whop-public-cli
```
> ⚠️ **لا تثبّت حزمة `whop` المجرّدة** — لمطوّر آخر ومهجورة منذ 2022.
> ولا تستخدم `curl | sh` هنا: سكربت لِلينكس على جهاز ويندوز.

### ⏸️ معلّق: تسجيل الدخول
النافذة الحالية ترث PATH قديماً، فـ`whop` وحده لا يُعرَف. **الحل — أعد تشغيل الجهاز مرة واحدة** ثم:
```bash
whop auth login
whop auth status      # لازم loggedIn: true
```
أو بلا إعادة تشغيل، بالمسار الكامل:
```bash
& "$env:APPDATA
pm\whop.cmd" auth login
```

### أول أمرين بعد الدخول
```bash
whop webhooks list    # ← فحص رابط الجسر والأحداث (البند المعلّق من تدقيق ما قبل التسويق)
whop stats            # ← مقارنة أرقام Whop بلوحتك
```

### الإعلانات — جاهزة في مجلد `ads/`
خمس صور 1080×1080 (`ad-1` … `ad-5`) بزوايا: التحوّل · اللصق الذكي · التوافق · الطموح · السعر.
المصدر `_ads.html` (مستثنى من Git) — عدّل وأعد التوليد بـEdge headless.

**النصوص الكاملة لكل إعلان موجودة في محادثة 10 أغسطس.**

روابط الوجهة — واحد لكل إعلان ليُقاس:
```
https://aqaridesign.com/en/?src=meta1 … meta5
```

### إعدادات الحملة المتفق عليها
| | |
|---|---|
| المنصة | Meta (Whop تُشغّلها، ليست شبكة إعلانات بذاتها) |
| الهدف | **Traffic** لا Sales — لا يوجد بكسل يتعلّم بعد |
| الميزانية | $20/يوم × 5-7 أيام للتعلّم |
| الاستهداف | مدينة واحدة (مثلاً Austin +25mi) · 28-60 · اهتمامات: Realtor · Zillow · NAR · Keller Williams · RE/MAX · eXp |

### ❌ ناقص قبل الصرف على الإعلانات
**لا يوجد Meta Pixel في الموقع.** بدونه لا تحسين ولا retargeting ولا قياس عائد داخل Meta.
يحتاج `Pixel ID` من Meta Events Manager ثم يُركَّب في الصفحات الأربع.
